{erl_opts, [debug_info]}.
{deps, []}.

%% Build the rust code via a compile hook
{pre_hooks, [{compile, "cargo build --manifest-path priv/Cargo.toml --release"}]}.
{post_hooks, [{compile, "cp priv/target/release/librustler_test.so priv"}]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [rustler_test]}
]}.
